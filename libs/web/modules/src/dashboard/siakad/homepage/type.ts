export type TDataCard = {
  desc?: string;
  jumlah?: string | number;
  background: string;
  tcolor: string;
  title: string;
  textColor?: string;
};
export type TNilai = {
  matkul: string;
  nilai: string;
};
