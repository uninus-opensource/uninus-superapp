export enum RADIO_SIZE {
  SM = "sm",
  MD = "md",
  LG = "lg",
}

export enum RADIO_VARIANT {
  PRIMARY = "primary",
  WARNING = "warning",
}

export enum LABEL_SIZE {
  SM = "sm",
  MD = "md",
  LG = "lg",
}
