import { FC, ReactElement } from "react";
import { TPagination } from "./type";

export const Pagination: FC<TPagination> = (): ReactElement => {
  return <div className="flex justify-center"></div>;
};
