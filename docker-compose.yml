version: '3.8'
services:
  database:
    container_name: uninus-db
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: uninus-db
    ports:
      - '5435:5435'
