import type { NextPage } from "next";
import type { ReactElement } from "react";
import { LoginModule } from "../_modules";

const LoginPage: NextPage = (): ReactElement => {
  return <LoginModule />;
};

export default LoginPage;
