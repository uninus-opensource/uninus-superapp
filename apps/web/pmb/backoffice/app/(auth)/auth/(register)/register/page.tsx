import type { NextPage } from "next";
import type { ReactElement } from "react";
import { RegisterModule } from "../_modules";

const RegisterPage: NextPage = (): ReactElement => {
  return <RegisterModule />;
};

export default RegisterPage;
